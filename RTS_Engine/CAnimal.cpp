#include "CAnimal.h"

class CAnimal
{
	bool IsAlive(tab){
		if(m_age > m_life_expectancy)
			return false;
		if(m_hunger == 0)
			return false;
		for(size_t i=0;i<std::size(tab);i++){
		    if ()
		}
		return true;
	}
}